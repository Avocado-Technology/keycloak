# From Infisical project "keycloak", env dev (see infra/modules/keycloak).
# CI exports these to .env.infisical before deploy. Do not commit real values.

KC_DB_URL=jdbc:postgresql://HOST:25060/keycloak?sslmode=require
KC_DB_USERNAME=keycloak
KC_DB_PASSWORD=
KEYCLOAK_ADMIN=admin
KEYCLOAK_ADMIN_PASSWORD=
KEYCLOAK_HOST=auth.dev.avcd.ai
